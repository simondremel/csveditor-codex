[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "csveditor-codex"
version = "0.1.0"
description = "CSV editor core engine"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
